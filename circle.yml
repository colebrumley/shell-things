dependencies:
  override:
    - sudo su -c 'echo "deb http://archive.ubuntu.com/ubuntu/ trusty-backports main restricted universe" >> /etc/apt/sources.list'
    - sudo apt-get -q update && sudo apt-get -qy install shellcheck make bash

test:
  override:
    - make test
